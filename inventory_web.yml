all:
  hosts:
    localhost:
      ansible_connection: local
web:
  hosts:
    webserver:
      ansible_user: rhel
      ansible_password: ansible123!
      ansible_connection: local
      ansible_host: localhost
      
